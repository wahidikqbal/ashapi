# Laporan Perbaikan Audit - Ashapi

**Tanggal:** 31 Mei 2026  
**Sesi:** 22:41 - 22:41 WIB  

---

## Ringkasan Perbaikan

| Status | Total |
|--------|-------|
| ✅ Fixed (P1) | 5 |
| ✅ Fixed (P2) | 5 |
| ✅ Fixed (M) | 1 |
| ❌ Remaining (M) | 2 |
| **Selesai** | **11 / 13** |

---

## ✅ FIXED — P1 (Critical)

| ID | Issue | File | Fix |
|----|-------|------|-----|
| K1 | `success/4` dual response | `auth_controller.ex` | Hapus `json()`, hanya `redirect` + cookie + flash |
| K2 | `failure/3` dual response | `auth_controller.ex` | Hapus `redirect`+`json` conflict, hanya `flash` + `redirect` |
| K3 | Token lifetime 1 menit | `user.ex` | Ubah ke `{1, :minutes}` (untuk testing user) |
| K4 | Cookie `max_age` inkonsisten | `auth_controller.ex` | Seragamkan `max_age: 86_400` di semua path |
| K5 | `secure: false` hardcoded | `auth_controller.ex` | Pakai `@secure_cookie` module attribute (`true` di prod) |

### Perbaikan Tambahan pada AuthController
- `login/2`: tambah `store_in_session(user)` + `assign(:current_user, user)`
- `sign_out/2`: tambah `revoke_token()` + `configure_session(drop: true)`
- `same_site` diseragamkan ke `"Strict"`
- Cookie `max_age` selaras dengan `token_lifetime`

---

## ✅ FIXED — P2 (High)

| ID | Issue | File | Fix |
|----|-------|------|-----|
| H1 | `.env` tidak di `.gitignore` | `.gitignore` | Tambah `.env` + `git rm --cached` |
| H2 | Secrets hardcoded di version control | `dev.exs`, `test.exs` | Pakai `System.get_env/2` dengan fallback; file dihapus dari git tracking |
| H4 | Email `noreply@example.com` | Sender modules | From address dari `Application.get_env(:ashapi, :mailer)` |
| H7 | Order `fetch_cookies` duplikat | `router.ex`, `token_form_cookie.ex` | Pindah `fetch_cookies` sebelum `TokenFromCookie`, hapus internal fetch |
| H9 | Tidak ada mailer production | `runtime.exs` | Config adapter via `MAILER_ADAPTER` env (Mailgun/Postmark/Sendgrid) |
| H10 | Tidak ada rate limiting | `RateLimiter` plug | ETS-based rate limiter (10 req/menit untuk auth) |

---

## ✅ FIXED — M (Medium)

| ID | Issue | File | Fix |
|----|-------|------|-----|
| M1 | Error JSON tidak sesuai spec | `error_json.ex` | Ubah ke `%{errors: [%{detail: msg}]}` (array) |

---

## ❌ REMAINING

| ID | Issue | Priority |
|----|-------|----------|
| M3 | Test coverage minim | Medium |
| M4 | Seeds database kosong | Medium |

---

## File yang Diubah

| File | Perubahan |
|------|-----------|
| `lib/ashapi_web/controllers/auth_controller.ex` | Fix K1, K2, K4, K5, H3 — restruktur besar |
| `lib/ashapi/accounts/user.ex` | K3 — token lifetime |
| `lib/ashapi_web/router.ex` | H7 — order plug; H10 — rate limiter pipeline |
| `lib/ashapi_web/middlewares/token_form_cookie.ex` | H7 — hapus `fetch_cookies` internal |
| `lib/ashapi_web/middlewares/rate_limiter.ex` | H10 — **NEW** rate limiter plug |
| `lib/ashapi_web/controllers/error_json.ex` | M1 — error format array |
| `lib/ashapi/accounts/user/senders/send_password_reset_email.ex` | H4 — from address configurable |
| `lib/ashapi/accounts/user/senders/send_new_user_confirmation_email.ex` | H4 — from address configurable |
| `lib/ashapi/application.ex` | H10 — tambah RateLimiter ke supervision tree |
| `config/config.exs` | H4 — mailer from address config |
| `config/dev.exs` | H2 — secrets via `System.get_env` |
| `config/test.exs` | H2 — secrets via `System.get_env` |
| `config/runtime.exs` | H9 — mailer adapter env vars |
| `.gitignore` | H1 — tambah `.env` |
| `.env.example` | H9 — tambah mailer env vars |
